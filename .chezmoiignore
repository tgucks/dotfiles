# Repo files that should not be deployed to ~
README.md
LICENSE
CLAUDE.md
docs/**
terminal/**
git/**
tests/**
obsidian/**

{{ if ne .machineType "work" }}
# Split identities only exist on work machines
.gitconfig-work
.gitconfig-personal
{{ end }}

{{ if .isServer }}
# Skip GUI/desktop configs on servers
.config/ghostty/**
.tmux.conf
{{ end }}
